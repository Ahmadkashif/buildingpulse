# buildingPulse Specs

Doc-first specs that drive the scaffold. Each file is intentionally terse — just enough for a future generator pass to rebuild the corresponding module.

```
specs/
├── models/        entity contracts (types + MSW-backed endpoints)
├── pages/         page-level layouts and data flow
└── components/    component catalog referenced by pages
```
