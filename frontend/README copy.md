# buildingPulse

Energy-forecasting UI template generated from the ui-template scaffold engine.

```bash
npm install
npm run dev
```

Open [http://localhost:3000/forecasting](http://localhost:3000/forecasting) to start the workflow.

See `CLAUDE.md` for architecture and `specs/` for entity / page specs.
